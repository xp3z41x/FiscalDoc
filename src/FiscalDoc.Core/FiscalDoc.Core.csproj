<Project Sdk="Microsoft.NET.Sdk">

  <!-- TFM neutro de proposito. Core nao pode conhecer System.Drawing nem WinForms;
       alvejar net10.0 (e nao net10.0-windows) torna isso impossivel de violar,
       em vez de ser apenas uma regra escrita. Ver plano 2.5. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

</Project>
